server:
  port: 8075

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/food_delivery_customer
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  application:
    name: Customer-Service